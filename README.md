This repository demonstrate a suspected bug in the nose tests finder tool.

To reproduce the bug :
* pull the repository
* set a python virtualenv and install requirements (basically nose)
* run nosetests
